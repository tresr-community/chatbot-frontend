---
// Layouts
import Layout from "../layouts/Layout.astro";

import {env} from "cloudflare:workers";

// Cloudflare Environment Variables
const title = env.TITLE ?? "TRESR Community ChatBot";
const footerMessage =
  env.FOOTER_MESSAGE ?? "Ron Jay, serving the TRESR community since 2024.";
const welcomeMessage = env.WELCOME_MESSAGE ?? "Hi, I'm Ron Jay.";
const chatbotApiVersion = env.AI_VERSION ?? "v1";
const chatbotApiBackend = env.AI_BACKEND ?? "chatbot";
const debug = env.DEBUG ?? false;

// chatbot Configuration
const chatbotType = "fullscreen";
const chatbotStyle = "light";
const chatbotShowButton = false;
const enableLoadingSpinner = true;
const enableQuotes = false;
---

<Layout
  title={title}
  footerMessage={footerMessage}
  welcomeMessage={welcomeMessage}
  chatbotType={chatbotType}
  chatbotStyle={chatbotStyle}
  chatbotApiVersion={chatbotApiVersion}
  chatbotApiBackend={chatbotApiBackend}
  chatbotShowButton={chatbotShowButton}
  enableLoadingSpinner={enableLoadingSpinner}
  enableQuotes={enableQuotes}
  debug={debug}
/>
